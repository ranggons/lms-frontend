import React from "react";
import { Route, Routes } from "react-router";

const Dashboard = () => {
	return (
		<div className="text-black bg-red-200 h-screen w-screen flex justify-center items-center">
			RANGON
		</div>
	);
};

export default Dashboard;
